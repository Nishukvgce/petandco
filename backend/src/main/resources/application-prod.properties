# Production Configuration for Render
server.port=${PORT:8081}

# Database Configuration - Neon PostgreSQL
spring.datasource.url=${SPRING_DATASOURCE_URL: jdbc:postgresql://dpg-d57rve63jp1c73b5dq6g-a:5432/pet_co?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:pet_co_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:LOk0VxEadnKiHCRoixxpsbZIcPtjR5ZK}
spring.datasource.driver-class-name=org.postgresql.Driver
SPRING_JPA_DATABASE_PLATFORM = org.hibernate.dialect.PostgreSQLDialect
# JPA/Hibernate Configuration for PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# File upload configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.com.eduprajna=INFO
logging.level.pet-co=WARN
logging.level.org.springframework.web=INFO

# Health check endpoint
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always

# Email Configuration (Production)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME}
spring.mail.password=${EMAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Application Configuration (Production)
app.frontend.url=https://petandcom.vercel.app/
app.email.from=${EMAIL_FROM:PET&CO <noreply@pet-cotraditional.in>}